{
  "name": "new-project-form",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/new-project-form/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/angular:ng-packagr-lite",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "apps/new-project-form/ng-package.json",
        "tsConfig": "apps/new-project-form/tsconfig.lib.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "apps/new-project-form/tsconfig.lib.prod.json"
        },
        "development": {}
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../coverage/apps/new-project-form"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
