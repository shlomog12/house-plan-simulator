import { Route } from '@angular/router';
import { NewProjectForm } from './new-project-form/new-project-form';

export const newProjectFormRoutes: Route[] = [
  { path: '', component: NewProjectForm },
];
